--MODULE SCRIPT, PARENT = MAIN.LUA

return {
	["Key"] = 69, --input key here, could be a string or a number, or really anything except bool
	["kickOnIncorrectKey"] = true, --if service attempts to be accessed by client but inputs an incorrect key, then kick the player
}
